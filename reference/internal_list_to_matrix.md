# Turn an internal list to a matrix

this function wrangles the internal list to character matrix to show how
each parameter relates across conditions

## Usage

``` r
internal_list_to_matrix(internal_list)
```

## Arguments

- internal_list:

  the internal list of a flex_prms object

## Value

a character matrix with all parameter labels (columns) and conditions
(rows)
