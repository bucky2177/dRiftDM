# Check if an object of length \<= 1 is either empty or contains entry with ""

Check if an object of length \<= 1 is either empty or contains entry
with ""

## Usage

``` r
is_empty(x)
```

## Arguments

- x:

  a single value or empty vector

## Value

TRUE or FALSE otherwise
